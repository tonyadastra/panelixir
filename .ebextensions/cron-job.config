files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            * * * * * root /var/www/html/update_news.py

commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/mycron.bak"